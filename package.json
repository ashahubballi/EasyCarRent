{
  "name": "easycarrent",
  "version": "1.0.0",
  "description": "Dapp for car rent",
  "main": "truffle.js",
  "directories": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "scripts": {
    "start": "cd src && http-server",
    "dev": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.3.0"
  },
  "dependencies": {
    "fs": "0.0.1-security",
    "http": "0.0.1-security",
    "http-server": "^0.12.3"
  }
}
